# alir 開発計画

## 目的

Claude Code による GitHub Issue の自律的な消化を最大化する。
レート制限に抵触しない範囲で 24 時間稼働させ、人間の判断が必要な場面ではループを止めずに非同期でやり取りする。

## 設計原則

- **人間の判断待ちでループを止めない**。
  判断が必要になった Issue は park して次の Issue に進み、回答が来たら再開する。
- **質問と回答は iceql ベースのローカルサービスで管理する**。
  質問の蓄積と人間の回答登録を alir 自身が持つ。GitHub Issue を質問チャネルとして使わない。
  ストレージが CSV + YAML なので、質問と回答の履歴を人間も Claude Code もそのまま読める。
- **状態はリポジトリに外在化する**。
  worktree とブランチで作業を隔離し、成果は必ずリモートへ push する。
  既定では PR を作り、直接 push 運用（push-branch 設定）のリポジトリでは指定ブランチへ直接 push する。
  セッションの再開に依存せず、Issue とリモートブランチと質問 DB を読み直せば続行できる状態を保つ。
- **質問は高影響の判断に限定する**。
  影響が小さい判断は推奨案で進め、PR 本文（直接 push 運用ではコミットメッセージ）に判断ポイントとして明記する。
  事前に聞くのは不可逆または高影響（スキーマ変更、外部 API 契約、削除系）だけとする。

## システム構成

alir は次のコンポーネントを持つ単一プロジェクトとして新規実装する（mogiri は拡張しない）。

- **質問サービス（ask_human）**：iceql をストレージとする質問管理サービス。
  MCP ツールとして Claude Code から質問を受け付け、CLI と Web UI から人間の回答を登録する。
  質問の登録時に Pushover とデスクトップへ通知する。
- **Issue レジストリ**：alir が消化対象の Issue を登録・管理するテーブル。
  ループドライバは GitHub を直接検索せず、このレジストリからキューを取得する。
- **ループドライバ**：Issue レジストリから Issue を取り、worktree を切って Claude Code をヘッドレス実行し、並列数と park/resume の状態遷移を管理する。
- **回答検知**：質問 DB を監視し、回答済みの質問に対応する Issue を再開キューに戻す。
- **稼働管理**：レート制限エラーへのバックオフに加え、公式の使用率を確認し、閾値を超えている間は新規実行を待機する。
- **質問規約（スキル）**：質問のフォーマットと質問してよい条件を Claude Code 側に強制するプロンプト規約。

## 開発するもの

### 1. 質問サービス（ask_human）

iceql をストレージとする質問管理サービスを作る。
質問 DB はひとつのディレクトリであり、git 管理して履歴を残せる。
iceql のデータディレクトリ（質問 DB と Issue レジストリ）は alir リポジトリの外に置き、パスは設定で指定する。

テーブルは questions の 1 本から始める。

- **id**：質問 ID。
- **issue**：対象 Issue（リポジトリ + 番号）。
- **session_id**：質問時の Claude Code セッション ID（`--resume` 最適化用）。
- **question / options / recommended / impact / timeout_action**：質問本体。
- **status**：open / answered / expired。
- **answer / answered_at**：人間の回答。
- **created_at**：質問日時。

インターフェースは 3 つ用意する。

- **MCP ツール**：Claude Code から呼ぶ `ask_human`。
  パラメータ（question, options, recommended, impact, timeout_action）をすべて必須とし、質問を INSERT して即座に成功を返す。
- **CLI**：人間が使う回答登録コマンド。
  未回答一覧の表示（`alir questions`）と回答の登録（`alir answer <id> <choice> [補足]`）を行う。
- **Web UI**：未回答質問の一覧を表示し、選択肢のボタンと補足テキストで回答を登録する画面。
  htmx などの軽量構成とし、LAN 内のスマホからアクセスできるようにする。
  選択肢のタップだけで回答が完了する形にする。

質問が登録されたら、Pushover とデスクトップ通知で人間に知らせる。
通知には Web UI の該当質問への URL を含め、スマホからそのまま回答できるようにする。

### 2. 質問規約スキル

ask_human の使い方をプロンプト側で強制するスキル（または CLAUDE.md 規約）を書く。

- 質問フォーマット：選択肢と推奨とデフォルトを必ず含め、選択肢の指定だけで返せる形にする。
- 質問してよい条件：不可逆または高影響の判断に限る。
- 低影響の判断：推奨案で進め、PR 本文の「判断ポイント」節（直接 push 運用ではコミットメッセージ）に記載する。
- park の作法：質問後は中途半端なコミットを残さず、現状を要約してから終了する。

### 3. Issue レジストリとループドライバ

消化対象の Issue を alir に登録するレジストリと、そこから PR 作成までを回すドライバを実装する。

Issue レジストリは iceql の issues テーブルとして持つ。

- 登録は CLI（`alir issues add <url>`）と Web UI から行う。
- GitHub のラベル検索による一括取り込み（`alir issues import --label <label>`）は補助コマンドとして用意してよいが、ループドライバは GitHub を直接検索しない。
- 優先度は priority カラムとして設け、状態（queued / running / parked / done）とあわせてレジストリで管理する。

ループドライバは次を行う。

- レジストリから queued の Issue を優先度順に取得する。
- Issue ごとに worktree とブランチを切り、`claude -p` で実行する。
- 成果は PR にする。CI が通らない PR は park に戻す。
- 並列数を制御する。

### 4. 回答検知と再開

質問 DB を監視し、回答が登録された Issue を再開する。

- 回答検知はポーリングで始める（ファイル監視は後回しでよい）。
- 再開は「新セッションで Issue と PR の現状と回答を読み直して続行」を基本とする。
- session_id による `claude -p --resume` は、長い調査の途中で park した場合の最適化として後から足す。
- timeout_action の実行：無回答のまま指定時間を過ぎた質問を expired にし、「推奨案で続行」が指定されていればその旨を記録して再開する。

### 5. 稼働管理

レート制限への受動的な対応と、使用量にもとづく能動的な待機の両方を持つ。

- レート制限エラー（429 / limit 到達）を検知し、指数バックオフで待つ。
  初期値は 1 分開始・2 倍・上限 1 時間とし、運用で調整する。
- 判定は `claude -p /usage` で取得する公式の使用率（5 時間セッション・週次・モデル別週次）で行い、いずれかのウィンドウが閾値を超えている間は新規 Issue の実行を止める。
  実行中のセッションは完了まで走らせてよい。
  公式の使用率はサーバー側の残枠をそのまま反映するため、手動セッションの消費も自然に織り込まれる。
- 閾値（既定 50%）は設定値として control テーブルに永続化し、Web UI から変更する。
  ドライバはサイクルごとに読み直すので、稼働中の変更も再起動なしで反映される。
- claude -p の結果 JSON に含まれる usage は補助記録として runs テーブルに残す（判定には使わない）。

## 開発順序

1. 質問サービス（MCP ツール + CLI の最小構成）
2. 質問規約スキル
3. 質問サービスの Web UI
4. Issue レジストリとループドライバ（park/resume の状態遷移を含む）
5. 回答検知と再開（ポーリング + timeout_action）
6. 稼働管理（バックオフ + 使用率閾値）
7. `--resume` による再開の最適化

1 と 2 だけでも、手動で回している Claude Code セッションの質問を非同期化できる。
3 以降で自律ループにする。

## 未決事項

すべて解消済み。決定内容は本文の各節に反映した。
